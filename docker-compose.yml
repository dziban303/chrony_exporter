---
version: '3'
services:
  chronyx:
    image: ghcr.io/dziban303/chronyx:latest
    pull_policy: always
    container_name: chronyx
    command:
      - '--chrony.address=host.docker.internal:323'
      - '--collector.sources'
    ports:
      - '9123:9123'
    restart: unless-stopped
